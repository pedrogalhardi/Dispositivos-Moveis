<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DoaçãoAlimentos" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DoaçãoAlimentos" parent="Base.Theme.DoaçãoAlimentos" />

    <!-- Estilo personalizado para o botão de login com o Google -->
    <style name="GoogleButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:drawableStart">@drawable/ic_google</item> <!-- Substitua por seu ícone -->
        <item name="android:drawablePadding">8dp</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_marginTop">16dp</item>
    </style>
</resources>
